import type { NextPage } from "next";
import SideNavigation from "../containers/sideNavigation";

const Home: NextPage = () => {
  return (
    <div>
      <SideNavigation />
    </div>
  );
};

export default Home;
